name: Linked Jobs with Approval
on:
  workflow_dispatch:   # or push, whichever you prefer

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step in Job 1
        run: echo "Running Job 1 - build or prepare artifacts"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    environment: prod   # <<-- job will pause for environment approval
    steps:
      - name: Step in Job 2
        run: echo "Job 2 runs only after environment approval by MakarandBhoir"
